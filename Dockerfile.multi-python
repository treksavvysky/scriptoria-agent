# Dockerfile for testing with different Python versions
FROM python:3.9-slim as python39
WORKDIR /app
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*
COPY requirements.txt setup.py ./
RUN pip install --no-cache-dir -r requirements.txt
COPY scriptoria/ ./scriptoria/
COPY tests/ ./tests/
RUN pip install -e .
RUN mkdir -p /app/workspace
ENV PYTHONPATH=/app SCRIPTORIA_WORKSPACE=/app/workspace
CMD ["python", "-m", "pytest", "tests/", "-v"]

FROM python:3.10-slim as python310
WORKDIR /app
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*
COPY requirements.txt setup.py ./
RUN pip install --no-cache-dir -r requirements.txt
COPY scriptoria/ ./scriptoria/
COPY tests/ ./tests/
RUN pip install -e .
RUN mkdir -p /app/workspace
ENV PYTHONPATH=/app SCRIPTORIA_WORKSPACE=/app/workspace
CMD ["python", "-m", "pytest", "tests/", "-v"]

FROM python:3.11-slim as python311
WORKDIR /app
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*
COPY requirements.txt setup.py ./
RUN pip install --no-cache-dir -r requirements.txt
COPY scriptoria/ ./scriptoria/
COPY tests/ ./tests/
RUN pip install -e .
RUN mkdir -p /app/workspace
ENV PYTHONPATH=/app SCRIPTORIA_WORKSPACE=/app/workspace
CMD ["python", "-m", "pytest", "tests/", "-v"]

FROM python:3.12-slim as python312
WORKDIR /app
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*
COPY requirements.txt setup.py ./
RUN pip install --no-cache-dir -r requirements.txt
COPY scriptoria/ ./scriptoria/
COPY tests/ ./tests/
RUN pip install -e .
RUN mkdir -p /app/workspace
ENV PYTHONPATH=/app SCRIPTORIA_WORKSPACE=/app/workspace
CMD ["python", "-m", "pytest", "tests/", "-v"]
